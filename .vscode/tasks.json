{
    // To run these tasks you need to have sass installed.
    // Install it with: npm install -g sass
    "version": "2.0.0",
    "tasks": [
        {
            "label": "sass compile",
            "type": "shell",
            "command": "sass --source-map --style=compressed ${workspaceFolder}/src/style-scss/style.scss ${workspaceFolder}/style.css",
            "windows": {
                "command": "sass --source-map --style=compressed ${workspaceFolder}\\src\\style-scss\\style.scss ${workspaceFolder}\\style.css",
                "options": {
                    "shell": {
                        "executable": "cmd.exe",
                        "args": [
                            "/d", "/c"
                        ]
                    }
                }
            },
        }
    ]
}